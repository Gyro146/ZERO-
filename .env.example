# ZERO — Discord Bot Token Configuration
# =========================================
# Copy this file to `.env` and fill in your values.
# NEVER commit `.env` to version control.
#
# Required:
DISCORD_TOKEN=
# Optional — scopes slash commands to a single guild (faster sync):
# DISCORD_GUILD_ID=
# Optional — logging verbosity:
# LOG_LEVEL=INFO
